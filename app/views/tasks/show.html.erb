<h1><%= @task.title %></h1>

<%= link_to "Edit #{@task.title}", edit_task_path(@task) %>

<p><%= @task.details %></h2>
<% if @task.completed == true %>
<input type="checkbox" checked ="<%= params[:checked]%>" >This task is completed
<% else %>
<input type="checkbox" checked ="<%= params[:checked]%>" >This task is not completed yet
<% end %>
<%= link_to "Back to Index", root_path %>
